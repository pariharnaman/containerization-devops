services:
  alpine:
    image: node:latest
    container_name: webapp
    ports:
      - "5000:5000"
    volumes:
      - ./html:/usr/share/nginx/html
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    restart: unless-stopped